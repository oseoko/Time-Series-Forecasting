"""
STEP1 covariate knowledge prompt — 채널 하나의 지식 record를 한 번에 생성.

한 채널의 train 관찰 전체를 한 프롬프트에 넣어 record를 1회 작성한다(채널당 LLM 1콜).
학습 대상 = 잔차(실제 − 타깃만 예측)가 '피처의 상태'에 따라 어떻게 갈리는가 = 이 피처의 기여.

핵심 가드: 잔차의 절대 수준이 아니라 상태 간 차이. '관련 없음'도 정당한 결론.
"""

knowledge_system = """# 역할
고정 TSFM이 {target}을 타깃 과거값만으로 예측하며, 이 피처는 보지 못합니다. 그래서 예측이 실제와 어긋난
몫에는 이 피처의 기여가 섞여 있습니다. 아래 이 피처의 train 관찰 전체를 근거로, 기여를 서술하는 record
하나를 작성하세요.

# 피처
이름: {channel}
설명: {description}

# 관찰 하나의 구성
관찰은 서로 독립된 7일 예측입니다 (window가 겹치지 않으니 시계열로 잇지 말고 독립 표본으로 다루세요). 각 관찰:
  이전28일 : TSFM이 예측 근거로 삼은 타깃 과거값 (이 피처는 보지 못함)
  피처값   : 이 피처의 값 (이력은 항상, horizon은 future-known만)
  예측     : TSFM이 이전28일만 보고 낸 값
  실제     : 실제 입국자 수
  잔차     : 실제 − 예측 (타깃 과거값만으로 설명 안 되는 몫 — 여기서 이 피처의 기여를 찾습니다)

관찰 전체를 함께 놓고 보세요. 피처값이 갈릴 때 잔차의 크기·방향도 함께 갈리는지가 핵심입니다.

# 판단
기여는 잔차의 절대값이 아니라 상태 간 차이입니다 — 한 상태가 −5천, 다른 상태가 −4천8백이면 공통으로 깔린
−4천8백은 예측 편향이고 이 피처의 기여는 −200입니다. 잔차만 보지 말고 예측·실제의 수준도 보세요 (5만대의
−5천과 2.5만대의 −5천은 다릅니다). 이전28일이 이미 추세를 담고 있으면 그 잔차는 예측 오차일 수 있으니
피처 기여와 구분하세요. 잔차에는 요일·휴일·계절도 섞이고 동행이 곧 인과는 아닙니다 — 근거가 얕거나
엇갈리면 "관련 없음"이라고 분명히 쓰세요. 기여를 지어내지 마세요.

# 출력
예보자가 읽고 곧바로 쓸 수 있는 지식을 한 문단으로 쓰세요. 중간 계산값이나 소수점까지의 수치를 나열하는
것은 지식이 아닙니다 — 당신이 판단한 결론을 쓰세요.

담을 것: 이 피처의 어떤 관측 가능한 상태에서 실제가 예측보다 높게(위로 기여) 또는 낮게(아래로 기여)
나오는지, 그 몫이 대략 어느 정도인지(범위로), 얼마나 확신하는지와 그 이유, 언제 기여가 사실상 없는지.

규모는 상태 간 차이로, 범위로 쓰세요. 특정일·하루치 최대 잔차는 쓰지 마세요. 예측 시점에 볼 수 있는
값으로만 조건을 거세요. 산문 한 문단, 반복 없이. 한국어로 작성하세요."""

knowledge_user = """# 이 피처의 train 관찰 전체 ({total}개, 서로 독립된 7일 예측)
{observations}"""
