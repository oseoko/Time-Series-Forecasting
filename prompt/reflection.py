"""
B-3 reflection prompt — 여기서 처음으로 GT 사용 (train에서만).

출력 = {"reflect": "...", "corrected": {피처: 라벨 또는 라벨 리스트}}.

corrected의 용도: case에는 원본 attribution과 corrected를 **함께** 저장합니다.
  - 검색 거리는 원본으로 계산합니다 (추론 시점엔 GT를 못 보므로, GT를 본 라벨과 비교하면 불공정).
  - corrected는 프롬프트에서 "그때 이렇게 봤는데 실제론 이게 맞았다"는 학습 재료로만 씁니다.
"""

reflection_system = """# 역할
정답(실제 입국자 수)이 밝혀졌습니다. 이번 window의 라벨 판정이 실제와 맞았는지 되짚으세요.
이 되짚음은 case와 함께 저장되어 앞으로 비슷한 상황의 판정에 쓰입니다.

# 근거는 잔차입니다
잔차 = 실제 − Ŷ_base. Ŷ_base는 타깃 과거값만 보고 만들어져 피처를 전혀 못 봤으므로, 잔차가 곧
피처들이 실제로 만들어낸 몫입니다. 이 몫이 그때 매긴 라벨과 맞아떨어지는지 보세요.

핵심은 **잔차가 어느 날에 몰렸는가**입니다.
- 잔차가 7일 내내 같은 방향으로 비슷하게 깔려 있으면, 그건 특정일 이벤트가 아니라 **Ŷ_base의 수준
  편향**입니다. 어떤 피처의 라벨도 이걸 근거로 올릴 수 없습니다.
- 잔차가 특정 날에 몰려 있고, 마침 그날 어떤 피처의 값이 특이했다면, 그 피처의 그날치 라벨이 맞습니다.

# reflect
어느 라벨이 맞았고 어느 라벨이 틀렸는지, **그렇게 판단한 근거(잔차의 모양과 피처값)**를 한국어
2~3문장으로 쓰세요.

쓰지 말 것:
- "다음엔 보정을 더 크게/작게 하세요" 같은 강도 조절 조언. 보정폭은 이 단계에서 정하지 않습니다.
- 이 시스템이 갖고 있지 않은 것에 대한 조언(예약 데이터, 교차검증, 선행지표 등).
- 보정이 좋았는지 나빴는지에 대한 총평. 그건 코드가 이미 계산합니다.

# corrected — 정답을 보고 다시 매긴 라벨
**모든 피처의 라벨을 다시 매기세요.** 라벨 = {labels}

- 원래 라벨이 옳았다고 보면 **그대로 두세요.** 대부분은 그대로일 것입니다.
- 잔차가 그 피처의 기여로 설명되지 않으면 `neutral`로 내리세요.
- 라벨을 올릴 때는 **그 피처의 값이 이번 window에서 실제로 특이했음**을 근거로 대세요. 다른 피처를
  전부 `neutral`로 내리면서 남은 한 피처에 잔차를 몰아주는 것은 원인 규명이 아니라 희생양 만들기입니다.
- 어느 피처의 값도 특이하지 않았다면 그 잔차는 Ŷ_base 자체의 오차이고, 라벨은 모두 `neutral`이 맞습니다.
- 출력 형식은 원본 attribution과 **똑같이** 맞추세요: future-known 피처는 길이 {horizon}의 리스트,
  past-only 피처는 문자열 하나.

JSON 키와 라벨 값은 영어 그대로 두고, reflect 본문만 한국어로 작성하세요."""

reflection_user = """# 예측 vs 정답 (향후 {horizon}일)
Ŷ_base  (타깃만)   : {y_base}
Ŷ_calib (보정 후)  : {y_calib}
GT      (실제)     : {gt}
잔차 (GT − Ŷ_base) : {resid}

# 그때 매긴 라벨 (attribution)
{attribution}

# 이번 window의 피처값 (라벨을 다시 매길 때 근거로 볼 것)
{cov_values}

# 그때의 판단 근거
{reasoning}"""
