# 파이프라인 실행에 필요한 패키지
#   pip install -r requirements.txt
# 검증 환경: Python 3.10

# TSFM (base 예측) — torch를 함께 끌어온다. GPU 없으면 CPU로 동작.
chronos-forecasting==2.2.2
torch==2.5.0

# LLM (attribution / calibration / reflection / explain)
openai==2.44.0

# 데이터
numpy==2.1.2
pandas==2.2.3
pyarrow==24.0.0        # master_daily.parquet 읽기

# dataset/build_dataset.py 로 원본 xlsx에서 재구축할 때만 필요
openpyxl==3.1.5
